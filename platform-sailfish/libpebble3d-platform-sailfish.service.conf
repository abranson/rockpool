[Service]
# The user service inherits the active session UID.  This tiny setgid launcher
# is the only component which gains the privileged group; it starts the daemon
# after dropping back to the session's real group and exposes no public socket.
ExecStart=
ExecStart=/usr/libexec/libpebble3d/libpebble3d-platform-sailfish-launcher
